Read `task.md` and the failing tests at `tests/test_expr.py`.

Implement `expr.py` so the tests pass.

Apply the **superpowers test-driven-development** skill: red-green-refactor
discipline, brainstorm edge cases beyond what the visible tests cover (the
spec in `task.md` is authoritative), watch each test fail before making it
pass, verify before completion. Use whatever superpowers subagents and
skills the workflow recommends (brainstorming, writing-plans,
executing-plans, dispatching-parallel-agents, verification-before-completion,
etc.) as appropriate.

Run `python -m pytest tests/test_expr.py -q` to confirm. Stop when green.
