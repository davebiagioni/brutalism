Read `task.md` and the failing tests at `tests/test_lru.py`.

Implement `lru.py` so the tests pass.

Apply the **brutalism** skill: minimal lines, no comments, no docstrings, no
helpers used once, no abstractions for hypothetical futures, standard library
only. Be brutal.

Run `python -m pytest tests/test_lru.py -q` to confirm. Stop when green.
