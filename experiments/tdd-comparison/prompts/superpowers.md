Read `task.md` and the failing tests at `tests/test_lru.py`.

Implement `lru.py` so the tests pass.

Apply the **superpowers test-driven-development** skill: red-green-refactor
discipline, watch each test fail before making it pass, verify before
completion. Use whatever superpowers subagents and skills the workflow
recommends (brainstorming edge cases, writing-plans, executing-plans,
verification-before-completion, etc.) as appropriate.

Run `python -m pytest tests/test_lru.py -q` to confirm. Stop when green.
